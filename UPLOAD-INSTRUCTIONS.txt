FLAT REPOSITORY FIX

Your media files are already in the repository root. This replacement index.html is written to match that exact flat layout.

1. In GitHub, open the current index.html.
2. Delete it, or upload this replacement index.html and choose Replace.
3. Upload .nojekyll to the repository root if it is not already there.
4. Commit the changes.
5. Wait for GitHub Pages to finish deploying, then hard refresh.

Do not rename or move the existing logo, photo, video, audio, or resource files.
